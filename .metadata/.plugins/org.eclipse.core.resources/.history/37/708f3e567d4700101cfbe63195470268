/* USER CODE BEGIN Header */
/**
  ******************************************************************************
  * @file           : main.c
  * @brief          : Main program body
  ******************************************************************************
  * @attention
  *
  * Copyright (c) 2025 STMicroelectronics.
  * All rights reserved.
  *
  * This software is licensed under terms that can be found in the LICENSE file
  * in the root directory of this software component.
  * If no LICENSE file comes with this software, it is provided AS-IS.
  *
  ******************************************************************************
  */
/* USER CODE END Header */
/* Includes ------------------------------------------------------------------*/
#include "main.h"
#include "adc.h"
#include "dma.h"
#include "tim.h"
#include "usart.h"
#include "gpio.h"

/* Private includes ----------------------------------------------------------*/
/* USER CODE BEGIN Includes */

#include "inttypes.h"
#include "stdbool.h"

#include "../../Drivers/MATH/math.h"

/* USER CODE END Includes */

/* Private typedef -----------------------------------------------------------*/
/* USER CODE BEGIN PTD */

/* USER CODE END PTD */

/* Private define ------------------------------------------------------------*/
/* USER CODE BEGIN PD */
//Number of PWM channels - R G B W => 4
#define NUMBER_OF_ANALOGS 4

//Values for mapping Potentiometer inputs
#define POT_MIN_VALUE	200
#define POT_MAX_VALUE	4095

#define PWM_MAX_VALUE	359
#define PWM_MIN_VALUE	0


/* USER CODE END PD */

/* Private macro -------------------------------------------------------------*/
/* USER CODE BEGIN PM */

/* USER CODE END PM */

/* Private variables ---------------------------------------------------------*/

/* USER CODE BEGIN PV */

uint32_t AD_RES_BUFFER[4] = {0};
uint32_t PWM_DC[4] = {0};
uint32_t PWM_DC_Scaled[4] = {0};


uint8_t ADCRead_FLAG;

/* USER CODE END PV */

/* Private function prototypes -----------------------------------------------*/
void SystemClock_Config(void);
/* USER CODE BEGIN PFP */

/* USER CODE END PFP */

/* Private user code ---------------------------------------------------------*/
/* USER CODE BEGIN 0 */

/* USER CODE END 0 */

/**
  * @brief  The application entry point.
  * @retval int
  */
int main(void)
{

  /* USER CODE BEGIN 1 */




/*
	bool goingUp_1 = true;
	bool goingUp_2 = true;
	bool goingUp_3 = true;
	bool goingUp_4 = true;

	int8_t delay = 1;
	bool goingUp_delay = true;
*/

  /* USER CODE END 1 */

  /* MCU Configuration--------------------------------------------------------*/

  /* Reset of all peripherals, Initializes the Flash interface and the Systick. */
  HAL_Init();

  /* USER CODE BEGIN Init */

  /* USER CODE END Init */

  /* Configure the system clock */
  SystemClock_Config();

  /* USER CODE BEGIN SysInit */

  /* USER CODE END SysInit */

  /* Initialize all configured peripherals */
  MX_GPIO_Init();
  MX_DMA_Init();
  MX_TIM1_Init();
  MX_ADC1_Init();
  MX_USART1_UART_Init();
  /* USER CODE BEGIN 2 */

  HAL_GPIO_WritePin(DEBUG_1_GPIO_Port, DEBUG_1_Pin, GPIO_PIN_RESET);

  HAL_ADC_Start_DMA(&hadc1, AD_RES_BUFFER, NUMBER_OF_ANALOGS);

  HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_1);
  HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_2);
  HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_3);
  HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_4);

  /* USER CODE END 2 */

  /* Infinite loop */
  /* USER CODE BEGIN WHILE */

//  uint16_t Counter = 0;


  while (1)
  {

/*
//	DEBUG - BLINK EACH COLOUR

	  if(Counter % 2 == 0)
		  HAL_GPIO_WritePin(LED1_GPIO_Port, LED1_Pin, GPIO_PIN_SET);
	  else
		  HAL_GPIO_WritePin(LED1_GPIO_Port, LED1_Pin, GPIO_PIN_RESET);

	  if(Counter % 3 == 0)
		  HAL_GPIO_WritePin(LED2_GPIO_Port, LED2_Pin, GPIO_PIN_SET);
	  else
		  HAL_GPIO_WritePin(LED2_GPIO_Port, LED2_Pin, GPIO_PIN_RESET);

	  if(Counter % 4 == 0)
		  HAL_GPIO_WritePin(LED3_GPIO_Port, LED3_Pin, GPIO_PIN_SET);
	  else
		  HAL_GPIO_WritePin(LED3_GPIO_Port, LED3_Pin, GPIO_PIN_RESET);

	  if(Counter % 5 == 0)
		  HAL_GPIO_WritePin(LED4_GPIO_Port, LED4_Pin, GPIO_PIN_SET);
	  else
		  HAL_GPIO_WritePin(LED4_GPIO_Port, LED4_Pin, GPIO_PIN_RESET);

	  Counter++;
*/

/*
//	DEBUG - GLOW-UP&DOWN EACH COLOUR

	  while(CH1_DC < 360)
	  	  {
	  		  TIM1->CCR1 = CH1_DC;
	  		  CH1_DC+= 1;
	  		  HAL_Delay(1);
	  	  }
	  	  while(CH1_DC > 0)
	  	  {
	  		  TIM1->CCR1 = CH1_DC;
	  		  CH1_DC -= 1;
	  		  HAL_Delay(1);
	  	  }

		  while(CH2_DC < 360)
		  	  {
		  		  TIM1->CCR2 = CH2_DC;
		  		  CH2_DC+= 1;
		  		  HAL_Delay(1);
		  	  }
		  	  while(CH2_DC > 0)
		  	  {
		  		  TIM1->CCR2 = CH2_DC;
		  		  CH2_DC -= 1;
		  		  HAL_Delay(1);
		  	  }

			  while(CH3_DC < 360)
			  	  {
			  		  TIM1->CCR3 = CH3_DC;
			  		  CH3_DC+= 1;
			  		  HAL_Delay(1);
			  	  }
			  	  while(CH3_DC > 0)
			  	  {
			  		  TIM1->CCR3 = CH3_DC;
			  		  CH3_DC -= 1;
			  		  HAL_Delay(1);
			  	  }

				  while(CH4_DC < 360)
				  	  {
				  		  TIM1->CCR4 = CH4_DC;
				  		  CH4_DC+= 1;
				  		  HAL_Delay(1);
				  	  }
				  	  while(CH4_DC > 0)
				  	  {
				  		  TIM1->CCR4 = CH4_DC;
				  		  CH4_DC -= 1;
				  		  HAL_Delay(1);
				  	  }
*/

/*
//	CUSTOM UP&DOWN 4-WAY BOUNCE - TBD

	  if(CH1_DC > 360)



		  goingUp_1 = false;
	  else if(CH1_DC < 0)
		  goingUp_1 = true;

	  if(CH2_DC > 360)
		  goingUp_2 = false;
	  else if(CH1_DC < 0)
		  goingUp_2 = true;

	  if(CH3_DC > 360)
		  goingUp_3 = false;
	  else if(CH1_DC < 0)
		  goingUp_3 = true;

	  if(CH4_DC > 360)
		  goingUp_4 = false;
	  else if(CH1_DC < 0)
		  goingUp_4 = true;




	  if(goingUp_1)
	  {
		  CH1_DC+= 1;
		  HAL_Delay(1);
	  }
	  else
	  {
		  CH1_DC-= 1;
		  HAL_Delay(1);
	  }

	  if(goingUp_2)
	  {
		  CH2_DC+= 3;
		  HAL_Delay(1);
	  }
	  else
	  {
		  CH2_DC-= 3;
		  HAL_Delay(1);
	  }

	  if(goingUp_3)
	  {
		  CH3_DC+= 5;
		  HAL_Delay(1);
	  }
	  else
	  {
		  CH3_DC-= 5;
		  HAL_Delay(1);
	  }

	  if(goingUp_4)
	  {
		  CH4_DC+= 10;
		  HAL_Delay(1);
	  }
	  else
	  {
		  CH4_DC-= 10;
		  HAL_Delay(1);
	  }



	  TIM1->CCR1 = CH1_DC;
	  TIM1->CCR2 = CH2_DC;
	  TIM1->CCR3 = CH3_DC;
	  TIM1->CCR4 = CH4_DC;



	  if(delay > 100)
		  goingUp_delay = false;
	  else if(delay < 0)
		  goingUp_delay = true;

	  if(goingUp_delay)
		  {
		  delay += 10;
			  HAL_Delay(1);
		  }
		  else
		  {
			delay -= 25;
			  HAL_Delay(1);
		  }

	  HAL_Delay(delay);
*/


/*
//	DEBUG - BLINK DEBUG_1

  HAL_GPIO_WritePin(DEBUG_1_GPIO_Port, DEBUG_1_Pin, GPIO_PIN_RESET);

  HAL_Delay(2000);

  HAL_GPIO_WritePin(DEBUG_1_GPIO_Port, DEBUG_1_Pin, GPIO_PIN_SET);

  HAL_Delay(500);
*/
if(ADCRead_FLAG)
{
//	Cut off bottom value to provide ability to turn LED's off
	PWM_DC[0] = Constrain(AD_RES_BUFFER[0], POT_MIN_VALUE, POT_MAX_VALUE);
	PWM_DC[1] = Constrain(AD_RES_BUFFER[1], POT_MIN_VALUE, POT_MAX_VALUE);
	PWM_DC[2] = Constrain(AD_RES_BUFFER[2], POT_MIN_VALUE, POT_MAX_VALUE);
	PWM_DC[3] = Constrain(AD_RES_BUFFER[3], POT_MIN_VALUE, POT_MAX_VALUE);

//	Scale values from 12-bit to 8/9-bit (max value 359 to achieve 200kHz frequency of PWM)
	PWM_DC_Scaled[0] = MAP(PWM_DC[0], POT_MIN_VALUE, POT_MAX_VALUE, PWM_MIN_VALUE, PWM_MAX_VALUE);
	PWM_DC_Scaled[1] = MAP(PWM_DC[1], POT_MIN_VALUE, POT_MAX_VALUE, PWM_MIN_VALUE, PWM_MAX_VALUE);
	PWM_DC_Scaled[2] = MAP(PWM_DC[2], POT_MIN_VALUE, POT_MAX_VALUE, PWM_MIN_VALUE, PWM_MAX_VALUE);
	PWM_DC_Scaled[3] = MAP(PWM_DC[3], POT_MIN_VALUE, POT_MAX_VALUE, PWM_MIN_VALUE, PWM_MAX_VALUE);

//	Write values into Timer register to set dutyCycle of PWM and therefore brightness of LED's
	TIM1->CCR1 = PWM_DC_Scaled[0];
	TIM1->CCR2 = PWM_DC_Scaled[1];
	TIM1->CCR3 = PWM_DC_Scaled[2];
	TIM1->CCR4 = PWM_DC_Scaled[3];

//	Reset Flag for next cycle
	ADCRead_FLAG = 0;
}






	  //for(Counter = 0 ; Counter >= 4093 ; Counter++)
	  //{}

    /* USER CODE END WHILE */

    /* USER CODE BEGIN 3 */
  }
  /* USER CODE END 3 */
}

/**
  * @brief System Clock Configuration
  * @retval None
  */
void SystemClock_Config(void)
{
  RCC_OscInitTypeDef RCC_OscInitStruct = {0};
  RCC_ClkInitTypeDef RCC_ClkInitStruct = {0};

  /** Configure the main internal regulator output voltage
  */
  HAL_PWREx_ControlVoltageScaling(PWR_REGULATOR_VOLTAGE_SCALE1);

  /** Initializes the RCC Oscillators according to the specified parameters
  * in the RCC_OscInitTypeDef structure.
  */
  RCC_OscInitStruct.OscillatorType = RCC_OSCILLATORTYPE_HSI;
  RCC_OscInitStruct.HSIState = RCC_HSI_ON;
  RCC_OscInitStruct.HSIDiv = RCC_HSI_DIV1;
  RCC_OscInitStruct.HSICalibrationValue = RCC_HSICALIBRATION_DEFAULT;
  RCC_OscInitStruct.PLL.PLLState = RCC_PLL_ON;
  RCC_OscInitStruct.PLL.PLLSource = RCC_PLLSOURCE_HSI;
  RCC_OscInitStruct.PLL.PLLM = RCC_PLLM_DIV1;
  RCC_OscInitStruct.PLL.PLLN = 8;
  RCC_OscInitStruct.PLL.PLLP = RCC_PLLP_DIV2;
  RCC_OscInitStruct.PLL.PLLR = RCC_PLLR_DIV2;
  if (HAL_RCC_OscConfig(&RCC_OscInitStruct) != HAL_OK)
  {
    Error_Handler();
  }

  /** Initializes the CPU, AHB and APB buses clocks
  */
  RCC_ClkInitStruct.ClockType = RCC_CLOCKTYPE_HCLK|RCC_CLOCKTYPE_SYSCLK
                              |RCC_CLOCKTYPE_PCLK1;
  RCC_ClkInitStruct.SYSCLKSource = RCC_SYSCLKSOURCE_PLLCLK;
  RCC_ClkInitStruct.AHBCLKDivider = RCC_SYSCLK_DIV1;
  RCC_ClkInitStruct.APB1CLKDivider = RCC_HCLK_DIV1;

  if (HAL_RCC_ClockConfig(&RCC_ClkInitStruct, FLASH_LATENCY_2) != HAL_OK)
  {
    Error_Handler();
  }
}

/* USER CODE BEGIN 4 */

void HAL_ADC_ConvCpltCallback(ADC_HandleTypeDef* hadc)
{
	ADCRead_FLAG = 1;
}

/* USER CODE END 4 */

/**
  * @brief  This function is executed in case of error occurrence.
  * @retval None
  */
void Error_Handler(void)
{
  /* USER CODE BEGIN Error_Handler_Debug */
  /* User can add his own implementation to report the HAL error return state */
  __disable_irq();
  while (1)
  {
  }
  /* USER CODE END Error_Handler_Debug */
}

#ifdef  USE_FULL_ASSERT
/**
  * @brief  Reports the name of the source file and the source line number
  *         where the assert_param error has occurred.
  * @param  file: pointer to the source file name
  * @param  line: assert_param error line source number
  * @retval None
  */
void assert_failed(uint8_t *file, uint32_t line)
{
  /* USER CODE BEGIN 6 */
  /* User can add his own implementation to report the file name and line number,
     ex: printf("Wrong parameters value: file %s on line %d\r\n", file, line) */
  /* USER CODE END 6 */
}
#endif /* USE_FULL_ASSERT */
